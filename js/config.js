// WireScout cloud configuration.
// Create a Supabase project, then replace these two values with your Project URL
// and publishable (anon) key. The publishable key is safe to use in a browser
// when Row Level Security is enabled using the included supabase-schema.sql.
window.WS_CONFIG = {
  SUPABASE_URL: "YOUR_SUPABASE_URL",
  SUPABASE_PUBLISHABLE_KEY: "YOUR_SUPABASE_PUBLISHABLE_KEY"
};
